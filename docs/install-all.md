# Install All Skills

```bash
python3 scripts/install_skills.py --all
```

Dry-run first:

```bash
python3 scripts/install_skills.py --all --dry-run
```

After installing, restart the Hermes session or use `/reset` so newly installed skills are visible.
