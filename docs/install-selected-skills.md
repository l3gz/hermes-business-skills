# Install Selected Skills

Install a pack:

```bash
python3 scripts/install_skills.py --pack marketing
```

Install one skill:

```bash
python3 scripts/install_skills.py --skill podcast-to-copy
```

Install to a specific Hermes profile:

```bash
python3 scripts/install_skills.py --pack marketing --target ~/.hermes/profiles/client/skills
```
