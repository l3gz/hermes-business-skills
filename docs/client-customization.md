# Client Customization

Use the generic skills as procedures and put client-specific facts in the client's wiki.

Example:

- `podcast-to-copy` explains the workflow
- the client wiki stores podcast transcripts, brand voice, approved claims, offers, examples, and avatar pages

If a client needs special behavior, create a small client overlay skill that references the generic skill and adds only the custom rules.
